% Add \pdfstrcmp into LuaLaTeX
% https://github.com/michal-h21/make4ht/issues/4#issuecomment-161023874
\usepackage{ifluatex}
\ifluatex
 \usepackage{pdftexcmds}
 \makeatletter
 \let\pdfstrcmp\pdf@strcmp
 \makeatother
\fi
\makeatletter

%----------------------------------------------------
%  The first part of this file is from 'rep12.sty'  |
%----------------------------------------------------
% rep12.sty 19-Jan-88

%==============================================================
% BEGIN Chapter Heading Customization
%==============================================================

\def\@makechapterhead#1{{\parindent 0pt
   \begin{center}
    %  \ifnum \c@secnumdepth >\m@ne \bf \@chapapp{} \thechapter \\ % TCM use this line to bold Chapter and Chapter Name
    \ifnum \c@secnumdepth >\m@ne  \@chapapp{} \thechapter \\ % TCM use this line to NOT bold Chapter and Chapter Name
     \vskip 2pt \fi    % TCM Increase space between chapter and chapter name
    %     \bf          %% Julie Use to boldface chapter name only
    #1
   \end{center}
   \nobreak } }

%-------------------------------------------------------------
% BEGIN TOC, LOF, LOT Chapter Headings Customization
%-------------------------------------------------------------
\def\@makeschapterhead#1{{\parindent 0pt
   \begin{center}
    %     \bf          %% Julie Use to boldface TOC, LOF, LOT chapter names
    #1
   \end{center}
   \nobreak } }
%-------------------------------------------------------------
% END TOC, LOF, LOT Chapter Headings Customization
%-------------------------------------------------------------


\def\chapter{\clearpage \thispagestyle{plain} \global\@topnum\z@
  \@afterindentfalse \secdef\@chapter\@schapter}

\def\@chapter[#1]#2{\ifnum \c@secnumdepth >\m@ne
 \refstepcounter{chapter}
 \typeout{\@chapapp\space\thechapter.}
 \addcontentsline{toc}{chapter}{\protect
  % \numberline{\hfill \thechapter. \hbox{\hskip 0.8em}\ignorespaces}#1}\else % TCM This code adds a ''0.8em'' in the PDF navigation tree for chapters but no error in the document.  Changed to line below. Not exactly equivalent code, but removes the ''0.8em'' in the PDF tree with no apparent side effects.
  \numberline{\hfill \thechapter. \hbox{\hspace{0.1in}}\ignorespaces}#1}\else
 \addcontentsline{toc}{chapter}{#1}\fi
% \vbox{\vskip 0.745in} %% Julie space betw. top of page and 'CHAPTER' if use plain
% \vbox{\vskip 0.66in} %% Julie space betw. top of page and 'CHAPTER' if use Bold
\vbox{\vskip 0.50in} %% saeed space betw. top of page and 'CHAPTER'
\chaptermark{#1}

%************************NEW CHAPTER DEFINITION*****************
\if@twocolumn
 \@topnewpage[\@makechapterhead{#2}]
\else \@makechapterhead{#2}
 \@afterheading\fi
% \vskip -0.27in  %% Julie add space betw. Plain chapter name and 1st sentence
\vskip -0.24in  %% Julie add space betw. Bold chapter name and 1st sentence
\chaptertest=0
\indent
\everypar={\advance \chaptertest by 1}
}

\def\@schapter#1{\if@twocolumn \@topnewpage[\@makeschapterhead{#1}]
 \else \@makeschapterhead{#1}
  \@afterheading\fi
 \indent
}

%==============================================================
% END Chapter Heading Customization
%==============================================================

%==============================================================
% BEGIN Section and subsection Customization
%==============================================================

%\def\section#1{\ifnum\chaptertest>1 \vskip 0.25in \else \vskip 0.01in \fi
\def\section#1{\ifnum\chaptertest>1 \vskip 0.25in \else \vskip 0.25in \fi
  \ifnum \c@secnumdepth >\m@ne
   \refstepcounter{section}
   \addcontentsline{toc}{section}{\protect
    \numberline{\thesection.}#1}\else
   \addcontentsline{toc}{section}{#1}\fi
  \sectionmark{#1}
  \noindent %T
  %T \centerline{$\underline{\hbox{\thesection.} _{\ } \ \hbox{#1}}$}
  \Null {\bf \thesection. \ #1}%T Removed preceeding space before bf which makes headers misaligned TCM
  \vskip -0.23in %% Julie
  \indent
 }

%\def\sectiondl#1#2{\ifnum\chaptertest>1 \vskip 0.25in \else \vskip 0.01in \fi
\def\sectiondl#1#2{\ifnum\chaptertest>1 \vskip 0.25in \else \vskip 0.25in \fi
 \ifnum \c@secnumdepth >\m@ne
  \refstepcounter{section}
  \addcontentsline{toc}{section}{\protect
   \numberline{\thesection.}#1 #2}\else
  \addcontentsline{toc}{section}{#1 #2}\fi
 \sectionmark{#1 #2}

 \centerline{$\underline{\hbox{\thesection.} _{\ } \ \hbox{#1}}$}
 \centerline{$\underline{\hbox{\thesection.} _{\ } \ \hbox{#1}}$}
 \centerline{$\underline{_{\ } \hbox{#2}}$}
 \vskip -0.09in %% Julie
 \indent
}

\def\subsection#1{\vskip 0.25in \ifnum \c@secnumdepth >\m@ne
  \refstepcounter{subsection}
  \addcontentsline{toc}{subsection}{\protect
   \numberline{\thesubsection.}#1}\else
  \addcontentsline{toc}{subsection}{#1}\fi
 \subsectionmark{#1}
 %T \centerline{\thesubsection. \ #1}
 \noindent %T
 \Null \thesubsection. \ #1
 \vskip -0.27in  %% Julie: space betw. name to the fisrt line
 \indent

}

\def\subsectiondl#1#2{\vskip 0.25in \ifnum \c@secnumdepth >\m@ne
  \refstepcounter{subsection}
  \addcontentsline{toc}{subsection}{\protect
   \numberline{\thesubsection.}#1 #2}\else
  \addcontentsline{toc}{subsection}{#1 #2}\fi
 \subsectionmark{#1}


}

\def\subsubsection#1{\vskip 0.25in \ifnum \c@secnumdepth >\m@ne
  \refstepcounter{subsubsection}
  \addcontentsline{toc}{subsubsection}{\protect
   \numberline{\thesubsubsection.}#1}\else
  \addcontentsline{toc}{subsubsection}{#1}\fi
 \subsubsectionmark{#1}
 \noindent
 %\centerline{\thesubsubsection. \ #1}  %% title is on the middle originally
 %T\Null \thesubsubsection. \ #1  %% Julie move it to the left ( standard requirement)
 \Null {\em \thesubsubsection. \ #1 } %% Julie move it to the left ( standard requirement) % Removed preceeding space before em which makes headers misaligned TCM
 %T \Null added to show first numeral
 \vskip -0.27in
 \indent

}

\def\subsubsectiondl#1#2{\vskip 0.25in \ifnum \c@secnumdepth >\m@ne
  \refstepcounter{subsubsection}
  \addcontentsline{toc}{subsubsection}{\protect
   \numberline{\thesubsubsection.}#1 #2}\else
  \addcontentsline{toc}{subsubsection}{#1 #2}\fi
 \subsubsectionmark{#1 #2}
 \noindent
 \centerline{\em \thesubsubsection. \ #1} \vskip -0.08in
 \centerline{\em #2}
 \vskip -0.27in
 \indent
}

%==============================================================
% END Section and subsection Customization
%==============================================================
%
%--------------------------------------------------------------------------
%  End of the first part and continue of the second part of "thesis.sty". %
%--------------------------------------------------------------------------
%

%======================================
% BEGIN TOC, LOF, and LOT Customization
%======================================

% TOC
\renewcommand\contentsname{TABLE OF CONTENTS} % Renames Table of Contents to all caps
\def\@pnumwidth{1.55em} % TCM Controls the distance between titles and page numbers in TOC
\def\@tocrmarg {4.00em} % TCM Controls the right margin of TOC
\def\@dotsep{1.5} % TCM Controls separation of dots in leaders
\setcounter{tocdepth}{3} % TCM Controls depth of TOC

\def\tableofcontents{\@restonecolfalse\if@twocolumn\@restonecoltrue\onecolumn
 \fi\chapter*{TABLE OF CONTENTS\@mkboth{CONTENTS}{CONTENTS}}
 \vskip -0.4in
 \@starttoc{toc}\if@restonecol\twocolumn\fi
}

%--------------------------------------------------------------
% BEGIN LOF Customization
%--------------------------------------------------------------
\def\loftitle{\dimen0=\hsize \advance\dimen0 by -4.8em
 \parshape=2 0pt\hsize 4.8em\dimen0}

\def\@dottedlofline#1#2#3#4#5{\ifnum #1>\c@tocdepth \else
  \vskip \z@ plus .2pt
  {\leftskip #2\relax \rightskip \@tocrmarg \parfillskip -\rightskip
  \parindent #2\relax\@afterindenttrue
  \interlinepenalty\@M
  \leavevmode
  \@tempdima #3\relax \advance\hangindent \@tempdima \hbox{}\hskip -\leftskip
  \loftitle #4\nobreak\leaders\hbox{$\m@th \mkern \@dotsep mu.\mkern \@dotsep
    mu$}\hfill \nobreak \hbox to\@pnumwidth{\hfil\rm #5}\par}\fi}
%--------------------------------------------------------------
% END TOC Customization
%--------------------------------------------------------------

\def\l@chapter{\par
 \baselineskip 0.25in  % added for smaller spacing
 \@dottedtocline{1}{0.0em}{2.6em}}
\def\l@section{\baselineskip 0.20in  % added for smaller spacing
 \@dottedtocline{1}{2.6em}{2.3em}}
\def\l@subsection{\baselineskip 0.20in  % added for smaller spacing
 \@dottedtocline{2}{4.9em}{3.2em}}
\def\l@subsubsection{\baselineskip 0.20in  % added for smaller spacing
 \@dottedtocline{3}{8.1em}{4.1em}}
\def\l@paragraph{\baselineskip 0.20in  % added for smaller spacing
 \@dottedtocline{4}{12.2em}{5em}}
\def\l@subparagraph{\baselineskip 0.20in  % added for smaller spacing
 \@dottedtocline{5}{17.2em}{6em}}
%--------------------------------------------------------------
% BEGIN LOF Customization
%--------------------------------------------------------------
\def\listoffigures{\@restonecolfalse\if@twocolumn\@restonecoltrue\onecolumn
  %%
  %% the 0.25in below was 0.625in before -- MARIO 06/96
  %%
 \fi \newpage \vbox{\vskip 0.25in \chapter*{LIST OF FIGURES\@mkboth %% uncommented by saeed and added comment below
   %  \fi \newpage \vbox{\vskip 0.515in \chapter*{LIST OF FIGURES\@mkboth  %% Julie
   {LIST OF FIGURES}{List of Figures}}}
 \vskip -0.3in
 \noindent
 Figure \hfill Page

 \addcontentsline{toc}{chapter}{\protect \noindent {LIST OF FIGURES}}

 \@starttoc{lof}\if@restonecol \twocolumn\fi}

\def\l@figure{\baselineskip 0.20in              % added for smaller spacing
 \@dottedlofline{1}{0.2em}{3.0em}} % in lists of figures

%--------------------------------------------------------------
% END LOF Customization
%--------------------------------------------------------------
%--------------------------------------------------------------
% BEGIN LOT Customization
%--------------------------------------------------------------
\def\l@table{\baselineskip 0.20in              % added for smaller spacing
 \@dottedlofline{1}{0.2em}{3.0em}} % in lists of tables

\def\listoftables{\@restonecolfalse\if@twocolumn\@restonecoltrue\onecolumn
  %%
  %% See above
  %%
 \fi \newpage \vbox{\vskip 0.25in \chapter*{LIST OF TABLES\@mkboth %% uncommented by saeed and added comment below
   %  \fi \newpage \vbox{\vskip 0.515in \chapter*{LIST OF TABLES\@mkboth %% Julie
   {LIST OF TABLES}{LIST OF TABLES}}}
 \vskip -0.3in
 \noindent
 Table \hfill Page

 \addcontentsline{toc}{chapter}{\protect \noindent {LIST OF TABLES}}

 \@starttoc{lot}\if@restonecol \twocolumn\fi}

\let\l@table\l@figure
%--------------------------------------------------------------
% END LOT Customization
%--------------------------------------------------------------
%------------------------------------------------------------------
% BEGIN Bibliography
%------------------------------------------------------------------
%% BEGIN thebibliography
\def\thebibliography#1{
 \newpage

 \vbox{ \vskip 0.545in   %% julie change for reference
  % \chapter*{REFERENCES\@mkboth
  \chapter*{\normalfont BIBLIOGRAPHY\@mkboth  % TCM REMOVED BOLD & CHANGED TO BIBLIOGRAPHY
   %% {BIBLIOGRAPHY}{BIBLIOGRAPHY} \vskip -0.7in }
   {BIBLIOGRAPHY}{BIBLIOGRAPHY} \vskip -0.5in }  %% tdp increase %space after
  %%     BIBLIOGRAPHY heading
  \refstepcounter{chapter}
  % \typeout{REFERENCES.}
  \typeout{BIBLIOGRAPHY.}                     % TCM Changed to Bibliography
  % \addcontentsline{toc}{chapter}{REFERENCES}
  \addcontentsline{toc}{chapter}{BIBLIOGRAPHY}% TCM Changed to Bibliography
  % \chaptermark{REFERENCES}
  \chaptermark{BIBLIOGRAPHY}                  % TCM Changed to Bibliography
 }  %% end of vbox

 \baselineskip 0.10in
 \renewcommand\lbaselinestretch{0.20in} %% tdp-fx single space %references

 \list
 {[\arabic{enumi}]}{\settowidth\labelwidth{[#1]}\leftmargin\labelwidth
  \advance\leftmargin\labelsep
  \usecounter{enumi}}
 \def\newblock{\hskip .11em plus .33em minus .07em}
 \sloppy\clubpenalty4000\widowpenalty4000
 \sfcode`\.=1000\relax}

%% END of thebibliography
%
% \let\endthebibliography=\endlist
%
%------------------------------------------------------------------
% END Bibliography
%------------------------------------------------------------------
%
%------------------------------------------------------------------
% BEGIN  Special caption style for table environment
%------------------------------------------------------------------
%
\def\tcaption{\refstepcounter\@captype \@dblarg{\@tcaption\@captype}}
\long\def\@tcaption#1[#2]#3{\par\addcontentsline{\csname
  ext@#1\endcsname}{#1} {\protect\numberline{\hfill \csname
   the#1\endcsname. \hbox{\hskip 0.5em}\ignorespaces}{\ignorespaces #2}}\begingroup
 \@parboxrestore
 \normalsize
 \@maketcaption{\csname fnum@#1\endcsname} {\ignorespaces #3}\par
 \endgroup}
\long\def\@maketcaption#1#2{
\vskip 10pt \baselineskip 0.23in
\setbox\@tempboxa\hbox{#1.-{\hskip 1pt}-#2}
\ifdim \wd\@tempboxa >\hsize #1.-{\hskip 1pt}-#2\par \else \hbox
 to\hsize{\hfil\box\@tempboxa\hfil}
 % \fi}
\fi \vskip 0.5cm}   %% Julie add   space betw. caption anf table

\long\def\@makecaption#1#2{
 \vskip 10pt \baselineskip 0.23in
 \setbox\@tempboxa\hbox{#1.\ \ #2}
 \ifdim \wd\@tempboxa >\hsize #1.\ \  #2\par \else \hbox
  to\hsize{\hfil\box\@tempboxa\hfil}
  % \fi}
 \fi \vskip 0.5cm}   %% Julie add   space betw. caption anf table

\long\def\@caption#1[#2]#3{\par\addcontentsline{\csname
  ext@#1\endcsname}{#1}{\protect\numberline{\hfill \csname
   the#1\endcsname.\hbox{\hskip 0.5em}\ignorespaces}{\ignorespaces #2}}\begingroup
 \@parboxrestore
 \normalsize
 \@makecaption{\csname fnum@#1\endcsname}{\ignorespaces #3}\par
 \endgroup}

\def\end@float{\vbox{\vskip 0.15in}
 \par\vskip\z@\egroup %% \par\vskip\z@ added 15 Dec 87
 \ifnum\@floatpenalty <\z@
  \@cons\@currlist\@currbox
  \ifdim \ht\@currbox >\textheight
   \ht\@currbox\textheight \fi
  \ifnum\@floatpenalty < -\@Mii
   \penalty -\@Miv
   \@tempdima\prevdepth    %% saving and restoring \prevdepth added
   \vbox{}                 %% 26 May 87 to prevent extra vertical
   \prevdepth \@tempdima   %% space when used in vertical mode
   \penalty\@floatpenalty
  \else \vadjust{\penalty -\@Miv \vbox{}\penalty\@floatpenalty}\@esphack
  \fi\fi}

\def\@xfloat#1[#2]{\ifhmode \@bsphack\@floatpenalty -\@Mii\else
  \@floatpenalty-\@Miii\fi\def\@captype{#1}\ifinner
  \@parmoderr\@floatpenalty\z@
 \else\@next\@currbox\@freelist{\@tempcnta\csname ftype@#1\endcsname
   \multiply\@tempcnta\@xxxii\advance\@tempcnta\sixt@@n
   \@tfor \@tempa :=#2\do
   {\if\@tempa h\advance\@tempcnta \@ne\fi
    \if\@tempa t\advance\@tempcnta \tw@\fi
    \if\@tempa b\advance\@tempcnta 4\relax\fi
    \if\@tempa p\advance\@tempcnta 8\relax\fi
   }\global\count\@currbox\@tempcnta}\@fltovf\fi
 \global\setbox\@currbox\vbox\bgroup
 %    \boxmaxdepth\z@     % commented out 15 Dec 87
 \hsize\columnwidth \@parboxrestore \baselineskip \fbaselinestretch}

\def\@startline{\vskip \tabbaselinestretch
 \global\@curtabmar\@nxttabmar\relax
 \global\@curtab\@curtabmar\global\setbox\@curline\hbox % missing \global
 {}\@startfield\strut}                                  % added 17 Jun 86

\def\list#1#2{\ifnum \@listdepth >5\relax \@toodeep
 \else \global\advance\@listdepth\@ne \fi
 \rightmargin \z@ \listparindent\z@ \itemindent\z@
 \csname @list\romannumeral\the\@listdepth\endcsname
 \def\@itemlabel{#1}\let\makelabel\@mklab \@nmbrlistfalse #2\relax
 \@trivlist \baselineskip \lbaselinestretch
 \parskip\parsep \parindent\listparindent
 \advance\linewidth -\rightmargin \advance\linewidth -\leftmargin
 \advance\@totalleftmargin \leftmargin
 \parshape \@ne \@totalleftmargin \linewidth
 \ignorespaces}

\def\@tabular{\leavevmode \hbox \bgroup $\let\@acol\@tabacol
 \let\@classz\@tabclassz
 \let\@classiv\@tabclassiv \def\arraystretch{\tbaselinestretch}
 \let\\\@tabularcr\@tabarray}
%------------------------------------------------------------------
% END  Special caption style for table environment
%------------------------------------------------------------------
%-------------------------------------------------------------
%-------------------------------------------------------------
% BEGIN Equation and Eqnarray Customization
%-------------------------------------------------------------
%----------------------------------------------------------------
%  Add more space around "equation" and "eqnarray" environment  |
%----------------------------------------------------------------
%
\@addtoreset{equation}{chapter}
\def\theequation{\thechapter.\arabic{equation}}

\def\eqnarray{\vskip 0.01in \stepcounter{equation}
 \let\@currentlabel=\theequation \global\@eqnswtrue
 \global\@eqcnt\z@\tabskip\@centering\let\\=\@eqncr
 $$\halign to \displaywidth\bgroup\@eqnsel\hskip\@centering
 $\displaystyle\tabskip\z@{##}$&\global\@eqcnt\@ne
 \hskip 2\arraycolsep \hfil${##}$\hfil
 &\global\@eqcnt\tw@ \hskip 2\arraycolsep
 $\displaystyle\tabskip\z@{##}$\hfil
 \tabskip\@centering&\llap{##}\tabskip\z@\cr}

\def\endeqnarray{\@@eqncr\egroup
 \global\advance\c@equation\m@ne$$
 \vskip 0.20in \noindent \global\@ignoretrue}

\def\equation{\vskip 0.01in $$ % $$ BRACE MATCHING HACK
 \refstepcounter{equation}}

\def\endequation{\eqno \@eqnnum % $$ BRACE MATCHING HACK
 $$ \vskip 0.10in \noindent \global\@ignoretrue}   %% 0.10in MARIO, 06/96
%-------------------------------------------------------------
% END Equation and Eqnarray Customization
%-------------------------------------------------------------

%\ps@plain \pagenumbering{arabic} \onecolumn \if@twoside\else\raggedbottom\fi

%----------------------------------------------------------%
%   The fourth part of this file is to define the special  %
%   display for the dissertation requirements              %
%----------------------------------------------------------%
%-------------------------------------------------------------
% BEGIN Page and Page Margins
%-------------------------------------------------------------
% All commands here is from the original smu_thesis.sty
% file and is commented out for an easier method using the
% package geometry.  The commands were not deleted for
% reference purposes. The page margin specifications are now
% placed in the template file's preamble (e.g. DedmanTemplate.tex)
% for ease of use.  Please refer to the geometry package
% documentation for further information - TCM
%-------------------------------------------------------------

% %\setlength{\topmargin}{-0.5in}
% %\setlength{\topmargin}{-0.142307692in}
% \setlength{\topmargin}{-0.27in}
% \setlength{\oddsidemargin}{0.5in}
% \setlength{\textwidth}{6.0in}
% \setlength{\textheight}{8.3in}
% %\setlength{\textheight}{8.6in}

%-------------------------------------------------------------
% END Page and Page Margins
%-------------------------------------------------------------

\parskip .15in  % Defines paragraph spacing

% Define the Front Pages, variables, and title formats

% \settable provided by Andreas Strauman
% https://tex.stackexchange.com/a/435611/152544
\makeatletter
\let\ea = \expandafter
\newcommand{\settable}[2][\@nil]{
%%% example call \settable[\actionIfNotSet]{test}

\ea\def\csname #2\ea\endcsname##1{
\ea\def\csname @#2\endcsname{##1}
\ea\def\csname @#2@noerror\endcsname{##1}
\ea\def\csname isset@#2\endcsname{1}
}
\ea\def\csname ifset@#2\endcsname##1##2{
\ifcsname isset@#2\endcsname%
 ##1
\else
 ##2
\fi
}

\ea\def\csname default@#2\endcsname{#1}%
\ifx#1\@nil\relax
 % If no default value given, then set @noerror to empty.
 \ea\def\csname @#2@noerror\endcsname{}%
\else
 \ea\def\csname @#2@noerror\endcsname{#1}%
\fi

\ea\ifx\csname default@#2\endcsname\@nnil
 \ea\def\csname default@#2\endcsname{
  \textbackslash #2%
  {\ea\@latex@warning{\ #2 not given}}
 }
\fi

\ea\def\csname @#2\endcsname{
 \csname default@#2\endcsname
}
}

\settable[SET AuthorFirstName]{AuthorFirstName}
\settable[SET AuthorLastName]{AuthorLastName}
\settable{AuthorNameSuffix}
\settable[SET GraduateDepartment]{GraduateDepartment}

\settable[SET ThesisDefenseDateYear]{ThesisDefenseDateYear}
\settable[SET ThesisDefenseDateMonth]{ThesisDefenseDateMonth}
\settable[SET ThesisDefenseDateDay]{ThesisDefenseDateDay}

\settable[SET GraduationDateYear]{GraduationDateYear}
\settable[SET GraduationDateMonth]{GraduationDateMonth}
\settable[SET GraduationDateDay]{GraduationDateDay}

\settable[Southern Methodist University]{University}
\settable[SET SMUCollege]{SMUCollege}

\newcount\numdegree
\newcount\numCmember

\def\Null{ }

\newcommand{\ThesisTitle}[3]{%
 \newcommand{\dtitleA}{#1}%
 \newcommand{\dtitleB}{#2}%
 \newcommand{\dtitleC}{#3}%
 \newcommand{\udtitle}{%
  \begin{center}
   \if\dtitleA\Null \else\uppercase{#1} \\ \fi
   \if\dtitleB\Null \else\uppercase{#2} \\ \fi
   \if\dtitleC\Null \else\uppercase{#3} \fi
  \end{center}%
 }%
}

\newcommand{\FirstDegree}{\numdegree=1%
 \settable[SET FirstDegreeType]{FirstDegreeType}%
 \settable[SET FirstDegreeMajor]{FirstDegreeMajor}%
 \settable[SET FirstDegreeUniversity]{FirstDegreeUniversity}%
 \newcommand{\DegreeA}{\@FirstDegreeType, \@FirstDegreeMajor, \@FirstDegreeUniversity}%
}
\newcommand{\SecondDegree}{\numdegree=2%
 \settable[SET SecondDegreeType]{SecondDegreeType}%
 \settable[SET SecondDegreeMajor]{SecondDegreeMajor}%
 \settable[SET SecondDegreeUniversity]{SecondDegreeUniversity}%
 \newcommand{\DegreeB}{\@SecondDegreeType, \@SecondDegreeMajor, \@SecondDegreeUniversity}%
}
\newcommand{\ThirdDegree}{\numdegree=3%
 \settable[SET ThirdDegreeType]{ThirdDegreeType}%
 \settable[SET ThirdDegreeMajor]{ThirdDegreeMajor}%
 \settable[SET ThirdDegreeUniversity]{ThirdDegreeUniversity}%
 \newcommand{\DegreeC}{\@ThirdDegreeType, \@ThirdDegreeMajor, \@ThirdDegreeUniversity}%
}
\newcommand{\FourthDegree}{\numdegree=4%
 \settable[SET FourthDegreeType]{FourthDegreeType}%
 \settable[SET FourthDegreeMajor]{FourthDegreeMajor}%
 \settable[SET FourthDegreeUniversity]{FourthDegreeUniversity}%
 \newcommand{\DegreeD}{\@FourthDegreeType, \@FourthDegreeMajor, \@FourthDegreeUniversity}%
}

\settable[Doctor of Philosophy]{DegreeSought}
\settable[SET GraduateDepartment]{GraduateDepartment}

\settable[SET AdvisorFullName]{AdvisorFullName}
\settable[SET AdvisorTitle]{AdvisorTitle}

\newcommand{\CommitteeMemberA}[1]{\numCmember=1 \newcommand{\memberA}{#1}}
\def\CommitteeMemberTitleA#1{\def\membertitleA{#1}}
\def\CommitteeMemberB#1{\numCmember=2 \def\memberB{#1}}
\def\CommitteeMemberTitleB#1{\def\membertitleB{#1}}
\def\CommitteeMemberC#1{\numCmember=3 \def\memberC{#1}}
\def\CommitteeMemberTitleC#1{\def\membertitleC{#1}}
\def\CommitteeMemberD#1{\numCmember=4 \def\memberD{#1}}
\def\CommitteeMemberTitleD#1{\def\membertitleD{#1}}
\def\CommitteeMemberE#1{\numCmember=5 \def\memberE{#1}}
\def\CommitteeMemberTitleE#1{\def\membertitleE{#1}}


\settable[SET Dedication]{Dedication}
\settable[Dissertation]{ThesisType}

\pagenumbering{roman}
%--------------------------------------------------------------------
% BEGIN Title Page
%--------------------------------------------------------------------
\def\Titlepage{
 \thispagestyle{empty}
 \raggedbottom
 {\baselineskip 0.35in
  %    \vbox{\vspace{0.64in}}
  \vbox{\vspace{0.1in}} %% Julie % changed by saeed was 0.482 changed to 0.1
  \udtitle
  \begin{center}
   A \@ThesisType~Presented to the Graduate Faculty of the
   \\
   \@SMUCollege \\
   \@University \\
   in \\
   Partial Fulfillment of the Requirements \\
   for the degree of \\
   \@DegreeSought \\
   with a \\
   Major in \@GraduateDepartment \\
   by \\
   \@AuthorFirstName~\@AuthorLastName~\@AuthorNameSuffix@noerror
  \end{center}}
 \vskip -0.30in   %% Here it was -0.40in, MARIO 06/96

 \vbox{\baselineskip0.20in
  \begin{center} \
   \ifnum\numdegree>0 \\ \DegreeA \fi
   \ifnum\numdegree>1 \\ \DegreeB \fi
   \ifnum\numdegree>2 \\ \DegreeC \fi
   \ifnum\numdegree>3 \\ \DegreeD \fi
   \\ \ \\ \  \\
   \vskip 0.118in     %% add by Julie
   \@GraduationDateMonth~\@GraduationDateDay, \@GraduationDateYear
  \end{center}}

 \newpage
 %\setcounter{page}{0}    %% commented out by saeed
}
%--------------------------------------------------------------------
% END Title Page
%--------------------------------------------------------------------
%--------------------------------------------------------------------
% BEGIN Approval Page
%--------------------------------------------------------------------
\def\Approvalpage{
 \thispagestyle{empty}  %added by saeed
 \setcounter{page}{1}
 \raggedbottom
 \vbox{\vspace{0.3in}}   %% 0.64 original julie %% changed by saeed to 0.3
 \udtitle
 \vspace{1.0in}
 \begin{flushright}
  \parbox{3.0in}{\begin{flushleft}
    \hfill Approved by: \\ \  \\
    %\vspace{0.1in} by saeed needed for 6 members
    \vspace{0.2in}
    \rule[-0.1in]{3.0in}{1pt} \\
    \@AdvisorFullName \vspace{-0.1in} \\
    \@AdvisorTitle \vspace{0.1in} \\
    \ifnum\numCmember>0 \rule[-0.1in]{3.0in}{1pt} \\
     %                   \memberA \vspace{0.1in}  % by saeed if 6 members change to 0.1 inch
     \memberA \vspace{-0.1in} \\ % TCM-changed vspace to -0.1in to accommedate titles per Dedman 2016 Standards, added \\, and moved \fi to line below
     \membertitleA \\
    \fi
    \ifnum\numCmember>1 \rule[-0.1in]{3.0in}{1pt} \\
     %                   \memberB \vspace{0.1in}  % by saeed if 6 members change to 0.1 inch
     \memberB \vspace{-0.1in} \\ % TCM-changed vspace to -0.1in to accommedate titles per Dedman 2016 Standards, added \\, and moved \fi to line below
     \membertitleB \\
    \fi
    \ifnum\numCmember>2 \rule[-0.1in]{3.0in}{1pt} \\
     %                   \memberC \vspace{0.1in}  % by saeed if 6 members change to 0.1 inch
     \memberC \vspace{-0.1in} \\ % TCM-changed vspace to -0.1in to accommedate titles per Dedman 2016 Standards, added \\, and moved \fi to line below
     \membertitleC \\
    \fi
    \ifnum\numCmember>3 \rule[-0.1in]{3.0in}{1pt} \\
     %                   \memberD \vspace{0.1in}  % by saeed if 6 members change to 0.1 inch
     \memberD \vspace{-0.1in} \\ % TCM-changed vspace to -0.1in to accommedate titles per Dedman 2016 Standards, added \\, and moved \fi to line below
     \membertitleD \\
    \fi
    \ifnum\numCmember>4 \rule[-0.1in]{3.0in}{1pt} \\
     %                   \memberE
     \memberE \vspace{-0.1in} \\ % TCM-changed vspace to -0.1in to accommedate titles per Dedman 2016 Standards, added \\, and moved \fi to line below
     \membertitleE
    \fi

   \end{flushleft}}
 \end{flushright}
 \newpage
 %\pagenumbering{alph}   %%commented out by saeed
 %\setcounter{page}{0}   %%commented out by saeed
}
%--------------------------------------------------------------------
% END Approval Page
%--------------------------------------------------------------------
%--------------------------------------------------------------------
% BEGIN Abstract Page
%--------------------------------------------------------------------
\newenvironment{Abstract}{\newpage
 %\addcontentsline{toc}{chapter}{\protect \noindent
 %{ABSTRACT}} %added by saeed -- include abstract in toc %% commented out by saeed after adding
 \noindent
 {\vbox{\baselineskip0.20in
   \ifnum\pdfstrcmp{\@AuthorNameSuffix@noerror}{}=0%
    \hbox{\@AuthorLastName, \@AuthorFirstName}%
   \else%
    \hbox{\@AuthorLastName~\@AuthorNameSuffix, \@AuthorFirstName}%
   \fi%
   \ifnum\numdegree>1 \hbox{\ } \fi
   \ifnum\numdegree>2 \hbox{\ } \fi
   \ifnum\numdegree>3 \hbox{\ } \fi
   \ifnum\numdegree=5 \hbox{\ } \fi}
  \hfill
  \vbox{\baselineskip0.20in
   \ifnum\numdegree>0 \hbox{\DegreeA} \fi
   \ifnum\numdegree>1 \hbox{\DegreeB} \fi
   \ifnum\numdegree>2 \hbox{\DegreeC} \fi
   \ifnum\numdegree>3 \hbox{\DegreeD} \fi   }} \\
 \vskip 0.04in   %% add by julie
 \noindent
 %%\if\dtitleA\Null \else $\underline{\hbox{\dtitleA { } \dtitleB}}$
 %%\fi %%modified by saeed - title in one line
 %%\vskip -0.20in    %% Julie
 %%\noindent
 %\if\dtitleB\Null \else $\underline{\hbox{\dtitleB} _{\ }}$ \fi %% commented out by saeed -- no need for two lines
 %\vskip -0.20in    %% Julie
 %\noindent
 %\if\dtitleC\Null \else $\underline{\hbox{\dtitleC}} _{\ }$ \fi %% commented out by saeed -- no need for two lines
 \if\dtitleA\Null \else $\underline{\hbox{\dtitleA} _{\ }}$ \fi
 \vskip -0.23in    %% Julie % TCM Changed from -0.20in per Dedman Standard
 \noindent
 \if\dtitleB\Null \else $\underline{\hbox{\dtitleB} _{\ }}$ \fi
 \vskip -0.23in    %% Julie % TCM Changed from -0.20in per Dedman Standard
 \noindent
 \if\dtitleC\Null \else $\underline{\hbox{\dtitleC}} _{\ }$ \fi
 \vskip -0.20in    %% TCM
 \noindent         %% TCM
 % \vspace{-0.05in}    %% mark by Julie
 \vspace{-0.009in}    %%  by Julie
 \vbox{\vskip0.120in \indent Advisor: \   \@AdvisorFullName  % TCM use above line if want Professor Prefix
  \par\vskip 0.02in}

 \vspace{-0.04in}  %x% Julie
 %       \vskip 0.20in    %% TCM Removed space per Dedman 2016 standard
 \noindent
 \vbox{\baselineskip 0.28in
  \hbox{\@DegreeSought~degree conferred \@GraduationDateMonth~\@GraduationDateDay, \@GraduationDateYear}

  \hbox{\@ThesisType~completed \@ThesisDefenseDateMonth~\@ThesisDefenseDateDay, \@ThesisDefenseDateYear}
  \vskip -0.10in}
 \parskip 0.00in

 \vbox{\vskip 0.25in}   %% add by Julie
 \indent
}{   }
%--------------------------------------------------------------------
% END Abstract Page
%--------------------------------------------------------------------
%--------------------------------------------------------------------
% BEGIN Copyright page per Dedman 2016 Standards TCM
%--------------------------------------------------------------------
\newenvironment{Copyright}{\newpage
 \thispagestyle{empty}
 \vbox{\vskip 0.5in}   % Sets top margin with line below
 \begin{center} Copyright (\the\year{}) \end{center}
 \begin{center} \vskip -0.125in \@AuthorFirstName~\@AuthorLastName~\@AuthorNameSuffix@noerror \end{center}
 \begin{center} \vskip -0.125in All Rights Reserved \end{center} \par
 \vskip 0.0in
}
%--------------------------------------------------------------------
% END Copyright page per Dedman 2016 Standards TCM
%--------------------------------------------------------------------
%--------------------------------------------------------------------
% BEGIN Acknowledgment Page
%--------------------------------------------------------------------

\newenvironment{Acknowledgment}{\newpage
 \parskip 0.00in
 %%
 %% Next line used to be \vbox{\vskip 0.63in} MARIO, 06/96
 %%
 \vbox{\vskip 0.5in} % TCM Changed to 0.5in per Dedman 2016 Standard
 %       \vbox{\vskip 0.63in} % TCM Changed to 0.5in per Dedman 2016 Standard
 %\addcontentsline{toc}{chapter}{\protect \noindent
 %{ACKNOWLEDGMENTS}} %added by saeed -- include ack in toc %% commented out by saeed after adding
 %T \addcontentsline{toc}{chapter}{\protect \noindent {ACKNOWLEDGMENTS}}
 %       \begin{center} \bf ACKNOWLEDGMENTS \end{center} \par    % TCM Removed Bold per Dedmand 2016 Standard
 \begin{center} ACKNOWLEDGMENTS \end{center} \par    % TCM Removed Bold per Dedmand 2016 Standard
 \vskip 0.0in % changed by saeed to 0.0 was 0.15
 \indent
}{  }
%--------------------------------------------------------------------
% BEGIN Acknowledgment Page
%--------------------------------------------------------------------
%--------------------------------------------------------------------
% BEGIN Symbols Page
%--------------------------------------------------------------------

\newenvironment{listofsymbols}{\newpage
 \vbox{\vskip 0.63in}
 \addcontentsline{toc}{chapter}{\protect \noindent {LIST OF SYMBOLS}}
 \begin{center} LIST OF SYMBOLS \end{center}
 \let\ttbaselinestretch\tabbaselinestretch
 \def\tabbaselinestretch{0.00in}
 \noindent
 \begin{tabbing}
  }{\end{tabbing}
 \let\tabbaselinestretch\ttbaselinestretch
 \newpage}
%--------------------------------------------------------------------
% BEGIN Symbols Page
%--------------------------------------------------------------------
\def\ApprovalTitlePages{
 \newcount\chaptertest
 \newcommand{\indexfile}{\jobname.idx}
 \immediate\openin7 \jobname.idx
 \ifeof7 \message{No file \indexfile}
 \else \immediate\closein7 \input{\indexfile} \fi
 \Approvalpage \Titlepage
 %\pagenumbering{roman} \setcounter{page}{3}  %%commented out by saeed
}

\newenvironment{thesis}{
 \parskip 0.00in
 \if\@Dedication\Null \else
  \newpage  \pagenumbering{alph}\setcounter{page}{3} \thispagestyle{empty} %% added by saeed
  \begin{center}
   \vbox{\vskip 0.803in} {\@Dedication}
  \end{center}
 \fi

 \addtocontents{toc}{\protect\noindent CHAPTER \hfill \protect\vskip 0.05in}
 \newpage
 \pagenumbering{arabic}
}

\newcommand\StartAppendix{
 \appendix
 \addtocontents{toc}{\protect\noindent APPENDIX \hfill
  \protect\vskip 0.05in}
}

%----------------------------------------------------------------------
%  The following definitions related to the baselineskip are for the  |
%  smaller spacing in float, list, tabular and tabbing environments   |
%----------------------------------------------------------------------
%
\def\fbaselinestretch{0.20in}
\def\lbaselinestretch{0.30in}  %% Will look like 2-space now MARIO,6/96
% originally there was 0.8 (below)
%%  \def\tbaselinestretch{0.30}
\def\tbaselinestretch{0.75}   %% change by Julie for table, heigh of row
\def\tabbaselinestretch{-0.07in}
%
% BEGIN Table Cell Height
%-------------------------------------------------------------
\renewcommand{\arraystretch}{1.3} % TCM Changes Vertical Cell height in tables. Default is 1
%-------------------------------------------------------------
% END Table Cell Height
%-------------------------------------------------------------


%-----------------------------
%  Initialize all variables  |
%-----------------------------
%
\numdegree=0
\numCmember=0

%----------------------
%  Other definitions  |
%----------------------
%
\def\refe#1{Eq.(\ref{#1})}
\def\reff#1{Figure \ref{#1}}
\def\reft#1{Table \ref{#1}}
\def\refch#1{Chapter \ref{#1}}
\def\refap#1{Appendix \ref{#1}}

\raggedbottom


%-------------------------------------------------------------
% BEGIN Command to print draft version of the thesis.
%-------------------------------------------------------------

\newcommand{\thesisdraft}{
 \pagestyle{fancy}
 \fancyhf{}
 \lhead{\bf DRAFT}
 \rhead{\bf DRAFT}
 \chead{\timestamp}
 \lfoot{\bf DRAFT}
 \cfoot{\thepage}
 \rfoot{\bf DRAFT}
 \renewcommand{\headrulewidth}{0.4pt}
 \renewcommand{\footrulewidth}{0pt}

 \fancypagestyle{plain}{
  \fancyhf{}
  \lhead{\bf DRAFT}
  \rhead{\bf DRAFT}
  \chead{\timestamp}
  \lfoot{\bf DRAFT}
  \cfoot{\thepage}
  \rfoot{\bf DRAFT}
  \renewcommand{\headrulewidth}{0.4pt}
  \renewcommand{\footrulewidth}{0pt}
 }
 \linenumbers
}
%-------------------------------------------------------------
% END Command to print draft version of the thesis.
%-------------------------------------------------------------


%-------------------------------------------------------------
% BEGIN Code to compute current timestamp.  Some parts by Martin Minow.
%-------------------------------------------------------------

\newcount\hour \newcount\minute
\hour=\time  \divide \hour by 60
\minute=\time
\loop \ifnum \minute > 59 \advance \minute by -60 \repeat
\def\thesistime{\ifnum \hour<13 \number\hour:%
  \ifnum \minute<10 0\fi
  \number\minute
  \ifnum \hour<12 \ am\else \ pm\fi
 \else \advance \hour by -12 \number\hour:%
  \ifnum \minute<10 0\fi
  \number\minute \ pm\fi}

\def\thesisdate{\ifcase\month\or
  January\or February\or March\or April\or May\or June\or
  July\or August\or September\or October\or November\or December\fi
 \space\number\day, \number\year}

\newcount\dow
\newcount\leap
\newcount\x
\newcount\y

%leap = year + (month - 14)/12
\leap=\month \advance\leap by -14 \divide\leap by 12
\advance\leap by \year

%dow = (13 * (month + 10 - (month + 10)/13*12) - 1)/5
\dow=\month \advance\dow by 10
\y=\dow \divide\y by 13 \multiply\y by 12
\advance\dow by -\y \multiply\dow by 13 \advance\dow by -1 \divide\dow by 5

%dow += day + 77 + 5 * (leap % 100)/4
\advance\dow by \day \advance\dow by 77
\x=\leap \y=\x \divide\y by 100 \multiply\y by 100 \advance\x by -\y
\multiply\x by 5 \divide\x by 4 \advance\dow by \x

%dow += leap / 400
\x=\leap \divide\x by 400 \advance\dow by \x

%dow -= leap / 100 * 2;
%dow = (dow % 7)
\x=\leap \divide\x by 100 \multiply\x by 2 \advance\dow by -\x
\x=\dow \divide\x by 7 \multiply\x by 7 \advance\dow by -\x

\def\thesisdayofweek{\ifcase\dow
  Sunday\or Monday\or Tuesday\or
  Wednesday\or Thursday\or Friday\or Saturday\fi}

\def\timestamp{\thesistime, \thesisdayofweek, \thesisdate}
%-------------------------------------------------------------
% END Code to compute current timestamp.  Some parts by Martin Minow.
%-------------------------------------------------------------
